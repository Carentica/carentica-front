name: carantica-front

on:
  push:
    branches: [main]

jobs:
  bluid:
    runs-on: debian-latest

steps:
    - name: Download projext from git
      uses: actions/checkout@v2

    - name: carantica-front
      run: |
      yarn
      yarn generate